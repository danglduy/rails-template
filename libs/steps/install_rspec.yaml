name: install_rspec
args:
  - version
cmds:
  - insert_into_file:
    - 'Gemfile'
    - "  gem 'rspec-rails', '~> %{version}'\n"
    - :after: "group :development, :test do\n"
  - run:
    - bundle install
  - run:
    - rails generate rspec:install
  - add_commit:
    - 'Add rspec'
